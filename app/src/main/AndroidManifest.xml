<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rcarb.backingapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>
        <activity android:name=".DisplayRecipesActivity"
            android:launchMode="singleTop"
            />
        <activity android:name=".RecipeStepDetailsActivity"/>

        <activity android:name=".PresentIngredientsActivity"
            android:launchMode="singleTop"
            />

        <provider
            android:authorities="com.example.rcarb.backingapp"
            android:name="com.example.rcarb.backingapp.Data.RecipesContentProvider"
            android:exported="false"/>
        
        <service android:name="com.example.rcarb.backingapp.Data.IntentServiceSQL"
            android:exported="false"/>
    </application>

</manifest>